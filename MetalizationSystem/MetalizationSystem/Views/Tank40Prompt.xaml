<Window x:Class="MetalizationSystem.Views.Tank40Prompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MetalizationSystem.ViewModels"
        mc:Ignorable="d"
        Title="Tank40Prompt" Height="450" Width="800">

    <!--<Window.DataContext>
        <local:Tank40PromptViewModel/>
    </Window.DataContext>-->
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Text="{Binding PromptMessage}" FontSize ="16"
                   TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"
                   Margin="0,0,0,20"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="加热停止并排液" Command="{Binding StartDrainCommand}"
                    Margin="5" Padding="15,5"/>
            <Button Content="排液关闭" Command="{Binding StopDrainCommand}"
                    Margin="5" Padding="15,5"/>
            <Button Content="进液使能" Command="{Binding StartChargeCommand}"
                    Margin="5" Padding="15,5"/>
            <Button Content="进液关闭" Command="{Binding StartChargeCommand}"
        Margin="5" Padding="15,5"/>
            <Button Content="继续运行" Command="{Binding ContinueCommand}"
                    Margin="5" Padding="15,5"/>    
        </StackPanel>
    </Grid>
</Window>
