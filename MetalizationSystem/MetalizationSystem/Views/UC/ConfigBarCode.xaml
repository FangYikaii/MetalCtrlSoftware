<UserControl x:Class="MetalizationSystem.Views.UC.ConfigBarCode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MetalizationSystem.Views.UC"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel  Margin="10" >
            <StackPanel Height="5"/>
            <Label Margin="10" Grid.Column="2" FontSize="20" FontWeight="Bold" Content="Config Bar Code" HorizontalAlignment="Left" Foreground="Black" />
            <DataGrid x:Name="ConfigList" ItemsSource="{Binding}" Margin="1" RowHeaderWidth="0" 
        GridLinesVisibility="Horizontal" FontSize="14" AlternationCount="2" 
        CanUserSortColumns="False" IsReadOnly="False"  CanUserAddRows="False" AutoGenerateColumns="False" 
        HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
        CanUserResizeColumns="False" CanUserResizeRows="False" CanUserReorderColumns="False" 
        BeginningEdit="List_BeginningEdit" CellEditEnding="List_CellEditEnding" 
        >
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                <Setter Property="Background" Value="#cbedfa" />
                            </Trigger>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#FFFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#76d7fb"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="LightBlue"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Resources>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="ExpId" Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Label x:Name="ExpID" Content="{Binding ExpID}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="FormulaId" Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Label x:Name="FormulaID" Content="{Binding FormulaID}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="FormulaName" Width="4*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Label x:Name="FormulaName" Content="{Binding FormulaName}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="BarCode" Width="4*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock x:Name="BarCode" Text="{Binding BarCode}" ToolTip="双击修改" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBox x:Name="BarCode_Edit" Text="{Binding BarCode}" Width="200" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <Button x:Name="btnCancelEdit" Content="×" Height="18" Width="18" Click="btnCancelEdit_Click"/>
                                    <Button x:Name="btnConfirmcEdit" Content="√" Height="18" Width="18" Click="btnConfirmcEdit_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Height="5"/>
            <StackPanel Height="5"/>
            <Button x:Name="btnConfigBarCode" Margin="10" HorizontalAlignment="Center" Grid.Row="1" Width="100" Height="30" Style="{StaticResource RoundedCornerButton}" IsEnabled="{Binding ConfigBarCodeIsEnable}" Click="btnConfigDone_Click" Tag="DownLoad">
                <Button.Content>
                    <Grid Width="100">
                        <TextBlock Text="Config Done" HorizontalAlignment="Center" Width="88" Margin="12,0,0,0" />
                    </Grid>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
